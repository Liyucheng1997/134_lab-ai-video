# 本项目在 Windows + NVIDIA(RTX 50 系) 上开发，默认使用 tools/f5-tts-env。
# 该环境已包含 CUDA 版 torch、F5-TTS、faster-whisper 和 Web 依赖。

# ASR
faster-whisper>=1.2

# TTS（含 torch / torchaudio，建议用 CUDA 版）
soundfile>=0.13
f5-tts>=1.1.20

# F5-TTS（本地·音色克隆）：
#   可通过 TTS_REF_AUDIO / TTS_REF_TEXT 做 zero-shot 音色克隆

# 翻译 / 下载
requests>=2.32
yt-dlp>=2026.6
biliup>=1.2
qrcode[pil]>=8.2

# Web 服务
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# 另需系统可执行：ffmpeg / ffprobe（项目默认用 tools/ 下的静态构建）
