# 本项目在 Windows + NVIDIA(RTX 50 系) 上开发，复用「114 听书软件」的 .venv（Python 3.11 + CUDA 12.8）。
# 若要独立安装，核心依赖如下（GPU 版 torch 请按官网指引装 cu12x 轮子）：

# ASR
faster-whisper>=1.2

# TTS（含 torch / torchaudio，建议用 CUDA 版）
f5-tts>=1.1
soundfile>=0.13
librosa>=0.11

# 翻译 / 下载
requests>=2.32
yt-dlp>=2026.6

# Web 服务
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# 另需系统可执行：ffmpeg / ffprobe（项目默认用 tools/ 下的静态构建）
